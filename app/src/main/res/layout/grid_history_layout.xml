<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/zero_transparent"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/no_data"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/strong_transparent"
        android:gravity="center"
        android:text="@string/history_unavailable"
        android:textColor="@color/gray"
        android:textSize="@dimen/no_data_size"
        android:visibility="gone" />

<GridView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/weather_grid_view"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/zero_transparent"
    android:columnWidth="@dimen/grid_column_width"
    android:gravity="center"
    android:horizontalSpacing="@dimen/screen_margin"
    android:numColumns="2"
    android:stretchMode="columnWidth"
    android:verticalSpacing="@dimen/screen_margin">

</GridView>
</LinearLayout>